---
description: The current ProjectRose version and the highlights from recent releases, kept in sync with what's actually shipped
---

<!--
  This file is a stub. The real body of the rose:patch-notes skill is built
  at load time so the version and highlight list always reflect the app the
  user is actually running. If you are reading this static text, the runtime
  generator failed.
-->

The runtime patch-notes body lists the current ProjectRose version and the headline changes from each release, in plain language. If you see this fallback instead, tell the user you couldn't read the release notes and offer to check what version they're on a different way.
